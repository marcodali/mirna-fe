// Write your WebSocketServer (wss) code and deploy it to the cloud for free!

wss.on('connection', (ws) => {
  ws.on('error', console.error)

  ws.on('message', (data) => {
    console.log(`soy zamas y tu deseo ${data.toString()} lo voy a cumplir`)
  })

  ws.send("hOLa soy el servidor Zamas wElCoMe!")
})
