// Write your WebSockets server code and deploy it to the cloud for free!

wss.on('connection', (ws) => {
  ws.on('error', console.error)

  ws.on('message', (data) => {
    console.log('soy zamas y voy a cumplir tu deseo:', data.toString())
  })

  ws.send('hOLa soy el servidor Zamas wElCoMe!')
})
